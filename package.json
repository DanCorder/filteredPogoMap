{
  "name": "tweet_filter",
  "version": "1.0.0",
  "description": "Follow a twitter user and retweet based on a filter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "with-local-db": "node-lambda run --configFile local.env",
    "with-live-db": "node-lambda run --configFile live.env"
  },
  "author": "Dan Corder",
  "license": "MIT",
  "dependencies": {
    "haversine": "^1.0.2",
    "twitter": "^1.7.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.38.0",
    "node-lambda": "^0.9.0"
  }
}
